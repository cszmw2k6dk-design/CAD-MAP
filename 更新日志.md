## 更新内容

- **修好了「LBD 一个标签都没填」**：以前只要选了「识别结果 JSON」，程序就走「JSON→标签行」这条路，**不再**从 PDF 里认 LBD 文字；而识别流程产出的 debug JSON 里没有标注框（shapes），于是写出一个空文件 → CAD 里就是「LBD 已填写 0 个标签」。现在按你说的分工走：
  - **LBD 标签**：Python 从 **PDF 文字层**认（就是老流程 `pdf_extract.py` 那套，已内置在程序里），位置就是图纸上 LBD 文字的位置；
  - **STR 支架号**：从识别结果 JSON 生成，按所属 LBD 分组、组内行优先编号（和 `frame_detect/map_lbd_str.py` 规则一致，每组从 01 起，不在任何 LBD 里的支架不编号），两组行写进同一个提取文件。
- **STR 号改成逐页画**：以前不管多少页，只有起始页那一页会画支架号（而且用的是"面积最大的底图"）；现在每张底图配自己的页号，86 页的项目就是 86 页都画。
- 修掉 CAD 侧那个弹窗 **「PDF布局工具错误: 类型不正确 - "\n[AI] no labels."」**：没有标签可画时，`c:pdfgridai` 会对一句字符串做 `(fix ...)`；现在正常提示"这次没有可画的标签"。
- 识别结果 JSON 里没有可填的标签时，日志会直接说明原因，不再静默写空文件。

## 上一版（2.17.7）

- 打包版**连不上 CAD（`No module named 'pythoncom'`）**修好了：打包用的 Python 里补装 pywin32，exe 里带上 `pythoncom` / `win32com`；少了 pywin32 时界面会直接提示，`build.py` 打包前也会检查
- 打包不再覆盖仓库里的 `Voltage-CAD MAP.spec`（生成的 spec 放进 `build/`）

## 更早（2.17.6）

- 界面下拉框（标签背景色 / STR 背景色 / STR 背景开关 / 支架明细行的「拆不拆」）**禁用滚轮切换**：鼠标停在上面滚轮不再改值，只能点开列表选；滚轮改成正常滚动页面，避免翻页时误改设置
- 界面去掉「**网格编号**」页：那几格（行数/列距/前缀）本来就不参与执行流程，真正的网格编号参数在 CAD 插件的 PDFGRID 对话框里设；「生成执行计划」里也不再列这一条
- 「支架」页新增「**命名前缀**」：支架号前缀填 **`STR`**（编号形如 `STR01`、`STR02`…），默认就是 `STR`；`CIR` 是网格编号用的前缀，不是支架号的
- 改了前缀会带到 CAD：识别结果 JSON 里形如 `STRxx` 的支架号按新前缀改名后再画（填 `CIR` 就画成 `CIR01`），CAD 端 STR 号的背景填充也跟着新前缀认
- 配置/ini 新增 `rackPrefix`
- 标签位置**固定模型空间**：选项页的「模型空间 / 当前布局」单选拿掉，ini 恒为 `labelWhere=M`（老配置是 `L` 也忽略）。填到布局要按当前布局的最大视口换算坐标，布局多/视口不规则容易错位，而且 STR 号本来就一直画在模型空间
- 生成布局后**按 LBD 区域上下限对准视口**（选项页新开关，默认开）：从识别结果 JSON 取每页 LBD 区域（Node 大框）的合并范围，插件按整页对准完之后再用这个范围重新对准一次视口 —— 居中 + 按区域上下限等比缩放。实测 LBD 区域只占页面高约 60%、宽 26%~42%，按整页对准图会偏小、留一圈空白；这一步是等比缩放（不会拉变形），拿不到识别结果时自动跳过

## 更早（2.17.4）

- 「执行输出」时**自动导出识别范围表**：LBD 区域范围（区域名、所属逆变器、区域内支架数）和 Typical（支架）范围，坐标同时给原图像素与归一化值；输出 `lbd_regions.csv`、`typicals.csv`、`lbd_symbols.csv`、`pages.csv`、`lbd_typical.json`
- 新增顶部「**导出区域范围**」按钮，可随时按识别结果导一次
- 「支架」页新增**支架类型明细**：每个支架类型单独一行（支架类型 / 串数 / 长度FT / 数量），**类型自动读识别结果，不用手填**；每行的**拆不拆自己选**（不拆 / 2行 / 3行）
- 支架类型**自动写入 ini**：点「执行输出」时程序会把类型补写进配置，这一次 CAD 侧就能读到，无需先手动刷新
- 新增选项开关：「生成时自动导出 LBD区域/支架范围」「支架类型自动读识别结果(免手填)」；新增「区域范围输出目录(可空)」
- 识别结果 JSON 自动查找：界面选的识别结果 → AI 输出目录 → 提取文件所在目录，取最新一份能解析的；找不到只提示一句，不打断流程
- 新增 `编排器/lbd_regions.py`（区域范围与支架类型提取，随 exe 一起打包）；`frame_detect/extract_lbd_typical.py` 改为它的命令行入口

## 附件

- `Voltage-CAD MAP.exe`（Windows 单文件版，直接替换旧 exe 即可；也可以在程序里点「检查更新」自动升级）
