## 更新内容

- 修复在线更新时**替换过程弹黑窗**的问题：改用内部等待，全程无窗口
- 修复更新完成后**自动重启报「Security validation failure」**：改为由资源管理器（explorer）启动新程序，不再从隐藏控制台拉起
- 修复顶部栏**下载进度数字导致的界面抖动**：状态文字、版本号、按钮改为固定宽度，进度右对齐
- 在线更新：程序顶部栏显示版本号，可「检查更新」；发现新版后一键下载并自动替换重启
- 更新日志：顶部栏「更新日志」按钮随时可看最新版本的说明；发现新版时弹窗显示完整更新日志
- 布局命名改为**按 LBD Excel 分表名**命名；复制数量仍按起始页/结束页
- DWG **不再自动保存**：执行完成后在界面点「选择保存位置并保存…」自行选路径
- 执行过程新增**分阶段进度**：创建布局 → 写 LBD 标签 → 画 STR 号
- 修复 STR 号画不出来的问题（ZWCAD `vla-GetBoundingBox` 返回 safearray 导致 AI 绘制中断）

## 附件

- `Voltage-CAD MAP.exe`（Windows 单文件版，直接替换旧 exe 即可；也可以在程序里点「检查更新」自动升级）
