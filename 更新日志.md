v2.50：更新内容
1 新增「按汇流箱(Box)象限定 STR 编号方向」：以本页汇流箱(Box)的几何中心为原点，
  看每个 LBD 区域的中心落在第几象限（I 右上 / II 左上 / III 左下 / IV 右下），
  四个象限各给一个下拉，选 8 种编号顺序里的一种；某个象限选「0 用全局顺序」就不改那一片
2 新增「象限预览」：把这一页真实的汇流箱 + LBD 区域按比例画出来，四个象限上底色、
  图例写当前选的顺序，每个区域按它落在哪个象限上色、框里写它的编号
3 兜底和以前一样：这页没有汇流箱(Box)、区域压在轴上（死区）、或者四个象限都选 0 时，
  还是按原来的「STR 编号顺序」走，输出和以前逐字节一致
4 这条规则带总开关：不勾「按汇流箱(Box)四象限规律命名」就整段不生效，STR 号一律按
  「STR 编号顺序」填（四个象限的选值留着，重新勾上就还是原来的）
5 界面分页重排：「PDF 与标签」拆成「PDF」和「标签」两页（PDF 页码/复制数量/视口边距/
  区域留白一页，标签高度、标签颜色、四象限规则一页）；四象限那几个下拉从「支架」页挪到
  「标签」页，和标签设置放一起
6 象限预览改成**规律示意图**：不用选识别结果也能看 —— 中间一个原点，四个象限各一小片
  3x3 格子，格子里的 01~09 就是这个象限当前那条顺序的走法（连线就是编号走向）；
  改下拉号立刻变，8 种顺序的差别一眼能看出来；总开关关掉时四个象限都按全局顺序画
7 「STR 顺序预览」以前格子多了会顶出控件、下半截看不见，现在按可用面积自动缩放，
  行/列再多也整片画得下
8 删掉「PDF 底图锁定」整个功能：老版本跑完会把 PDF 底图收进「PDF底图」层并锁住，
  在模型空间改标号时点不到底图，但移动/缩放底图又会被锁层挡住；现在 PDFLOCK / PDFUNLOCK
  两个命令、自动收层上锁、以及导入排列前的解锁全部删除，插件不再碰这个图层。
  已经跑过老版本、底图被锁在「PDF底图」层里的图纸，在 CAD 里用图层管理器（LAYER）
  手动解锁那一层一次即可，之后不会再被锁上

上一版（2.40）
1 STR 号避让更彻底：以前只按「文字框」避让底图上本来就印着的 LBD 编号，现在连号的背景白底一起算，
  压住就继续上下挪；整排找不到公共位置的，改成这一排的号各自单独挪
2 新增「LBD 标签反过来避让 STR 号」：STR 号画完后自动跑一趟，用 CAD 里真实的包围盒判断，
  把压住号的 LBD 标签上下错开（手动执行 LBDAVOIDSTR 可以随时再跑一遍）
3 只画 STR 前缀的号：识别结果里带的类名（Node9、Typical 之类）不会再被画到图纸上
4 底图自动收进「PDF底图」层并锁定：在模型空间里改标号时不会误点到压在最下面的 PDF 底图；
  要移动或缩放底图先执行 PDFUNLOCK，改完执行 PDFLOCK 锁回去
5 新增「导出 PDF」：勾选要导的布局 + 选 PDF 打印设备和纸型，逐张打印后合并成一份多页 PDF
6 更新下载改成测速选源 + 卡住自动换源 + 断点续传，并显示已下载大小和速度
7 界面「目标 DWG 文件」改名「模板 DWG 文件」

上一版（2.30）
1 修复 STR 号避让失效
2 加 STR 号避开 CAD 里画的 LBD 标签
3 生成布局后按 LBD 区域上下限对准视口过程加进度条
4 删掉没用的「使用AI自动识别(无需手动框)」和「锁定视口显示」两个选项
5 「执行输出」按钮挪到那一行的最右边

上一版（2.20.1）
1 修复更新自动保存json文件的bug

上一版（2.20）
1 修复支架拆分失效
2 删除多余没用按钮
3 加入自动把STR号放在同一高度功能
