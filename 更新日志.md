## 更新内容

- 新增**在线更新**：程序顶部栏显示版本号，可「检查更新」；发现新版后一键下载并自动替换重启
- 新增**更新日志**：顶部栏「更新日志」按钮随时可看最新版本的说明；发现新版时弹窗显示完整更新日志
- 布局命名改为**按 LBD Excel 分表名**命名，删除原「命名规则」页；复制数量仍按起始页/结束页
- DWG **不再自动保存**：执行完成后在界面点「选择保存位置并保存…」自行选路径（保存发生在 STR 画完之后，STR 号会一起存进文件）
- 执行过程新增**分阶段进度**：创建布局 → 写 LBD 标签 → 画 STR 号，状态行与进度条按阶段显示
- 修复 STR 号画不出来的问题（ZWCAD `vla-GetBoundingBox` 返回 safearray 导致 AI 绘制中断）
- 打包脚本修正：`PdfLayout_ai.lsp` 一并打进 exe；`build_exe.bat` 编码修正，双击即可运行

## 附件

- `Voltage-CAD MAP.exe`（Windows 单文件版，直接替换旧 exe 即可；也可以在程序里点「检查更新」自动升级）
