## 更新内容

- **「按 LBD 区域对准视口」的留白可以调了**：「PDF 与标签」页最下面新增 **「区域对准留白(%)」**，默认 **90**。它决定按 LBD 区域上下限对准视口时图纸占多大：**100 = 铺满视口（最大）**，越小图纸越小、四周留白越多（**95 = 上一版的行为，90 = 现在默认，比原来小一点点**）。填完直接重新生成即可，不用改代码。
  - 实现：生成时把界面这个数写进 CAD 的全局 `*PdfLayout_ViewInset*`（插件里就是靠它算缩放的：`缩放 = min(视口宽 × 留白 ÷ 区域宽, 视口高 × 留白 ÷ 区域高)`；90 → 图纸尺寸约为上一版的 94.7%）。
  - 提醒：这个系数同时也作用在「布局建好时按整页对准」那一次上，所以整页面也会跟着略小一点；真正决定成品大小的是区域那一次对准（它跑在整页对准之后）。

## 上一版（2.17.13）


- **LBD 标签改成上下两档错行（0、−1 行）**：同一排里从左到右，第 1 个落在区域中心线上，第 2 个往下错一行，第 3 个回到中心线，第 4 个再往下……错开量 = 1.5 倍字高（且不小于字高 + 0.10），保证**错开后不会压住上一个标签**。顺带修掉两个根因：
  - 以前是「撞一次往下 +1 档、再撞往上 −1 档、再 +2、−2…」，档位越攒越多 → 看着就是一个上一个下、越错越远；
  - 以前「换排」判定写死 0.02（约占页高 2%），而同一排里标签的高低差能到 0.028，**画到一半就被判成新的一排**，重新从中心线起步 → 更乱。现在按 **4 倍字高** 判排，并且先把这一页的标签按「排（上→下）+ 行内 x（左→右）」排好再画。
  - 实测（第 23 页 16 个 LBD 标签）：改前 8 对标签互相压住，改后 0 对。
- **STR 编号顺序可选 + 界面预览**：加 8 种顺序下拉（和 CAD 插件 PDFGRID 一致：列优先/行优先 × 左→右/右→左 × 上→下/下→上），并加「刷新 STR 顺序预览」——把一个 LBD 组里的支架按当前顺序画成格子示意（绿=第 1 个、橙=最后一个，连线=编号走向）。每个 LBD 组都从 01 重新编号。
- **生成时不再自动导出 LBD 区域 / 支架范围**（选项页有开关，默认不勾）；要导出就点底部「导出区域范围」。支架类型仍从识别结果 JSON 自动读、自动写进 ini。
- **区域范围文件按 Excel 分表顺序写**：布局名来自分表名，所以 R 行也按分表顺序排，保证「按 LBD 区域对准视口」时 R 行与布局一一对应（修对准对错页）。
- **进度与下载**：去掉没用的进度方块、只留进度条；LBD 标签和 STR 号都**按页码显示进度**；生成完成后右下角出现「**下载**」按钮，自己选保存位置。
- **STR 字高 / 背景色**：「支架」页新增「STR 字高」（填 0 = 自动：按 typical 框短边 × 1.4；填数字 = 固定字高），STR 背景色默认黄色；「标签高度」那一格只管 LBD 标签，不再连带改 STR 号。
- 插件读 L 行时带上**角度和字高**（第 5/6 列），CAD 端可以按行取字高。

## 上一版（2.17.12）


- **修 STR 号整体往右偏**：STR 号画完后原来会用**包围盒**再做一次居中校正，而 ZWCAD 对**旋转过（竖排 90°）的 MText** 返回的包围盒不准 → 整排号被推偏（实测偏约一个支架宽，正好是文字自身宽度）。现在改成和 LBD 标签同一套定位：**中点对齐 + 插入点=目标点**，只有不旋转的文字才再用包围盒校一次。这样每个 STR 号的几何中心就落在对应支架（Tracker）框的几何中心上。
- 自检参考：`pdfgridaiinfo` 打印的底图框是 `宽 36 × 高 24`（横版，与图纸一致）→ 说明坐标换算基准本来就是对的，问题只在文字定位这一步。

## 上一版（2.17.11）

- **LBD 标签改成填在「识别到的 LBD 区域」上**（选项页新开关「LBD 标签按识别到的区域位置填」，默认开）：
  以前 LBD 位置是从 **PDF 文字层**里找的，但你这套图纸的 LBD 名都排在**图纸右上角的清单表**里 → 标签全跑到角落里（你截图那样）。
  现在直接用识别结果里每个 **LBD 区域（Node 框）的中心**，配上 OCR 认出来的 LBD 号（实测全册 **1359 个区域，1359 个都有号**）→ 标签落在每个 LBD 上。
  顺带这一步**不用再跑 PDF 文字提取**，生成标签快很多。想回到老行为就把这个勾取消。
- LBD 填写加了诊断：CAD 进度文件里会写 `LBD_STAT 匹配到页=… 分表名没认出=… 编号表里没有=… 分表数=…`。

## 上一版（2.17.10）

- **修好「LBD 标签一个都填不上」的真凶**：插件里认分表名的函数 `PdfLayout_LbdSheetFromText` 写死了「INV + 2位数字 + A/B + **2位**数字」这种 7 字符格式（`INV01A01`）。你的项目是 **`INV11A101`**（尾段 3 位），被截成 `INV11A10`，跟 Excel 里的分表名 `INV11A101` 对不上 → 查不到标签文字 → 按设计"查不到就不画"，所以一条都没有。现在改成**变长解析**（每段位数不限），`INV01A01` 和 `INV11A101` 都认；顺带把返回值统一成大写（以防 PDF 文字是小写）。
- LBD 填写加了诊断：CAD 进度文件里会写一行 `LBD_STAT 匹配到页=x 分表名没认出=y 编号表里没有=z 分表数=n`，万一对不上，一眼能看出卡在哪一步。
- （同一批）标签页号按底图顺序重编号（修页号错位）、支架号字高默认自动（修"画了看不见"）。

## 上一版（2.17.9）

- **修好「LBD 已填写 0 个标签」（根因：页号对不上）**：CAD 侧是按**第几张底图**当页号的，而提取文件里写的是 **PDF 真实页码**。你的 PDF 前 22 页是封面/说明，图纸在 23~108 页，CAD 里只导入 86 张底图，两边一错位，标签就一条也落不下去。现在程序会按**识别到的图纸页、页码升序重编号 1~N**，和底图顺序严格对应；顺带把封面/说明页上的样板文字（`INVXXXXXX-LBD-XX`、`(18) STRINGS PER LBD MAX` 之类）自动滤掉。
- **修好「STR 号画了但图上看不见」**：支架号字高以前是固定的 0.15（模型单位），图纸一大就等于没有。现在默认**按支架框自动算字高**（字号≈支架短边），看得见也跟图成比例；想固定就照旧在「标签高度」填数字。
- 「标签高度」默认值从 `0.15` 改成 `0`＝自动（LBD 标签也跟着自动，谁填谁用）。
- 生成标签时日志会写清：`标签 N 行：LBD x 个（Python 从 PDF 文字层识别）+ 支架号 y 个（按 LBD 分组行优先编号）；识别到 86 页图纸，已按底图顺序重编号 1~86`。

## 上一版（2.17.8）

- **修好了「LBD 一个标签都没填」**：以前只要选了「识别结果 JSON」，程序就走「JSON→标签行」这条路，**不再**从 PDF 里认 LBD 文字；而识别流程产出的 debug JSON 里没有标注框（shapes），于是写出一个空文件 → CAD 里就是「LBD 已填写 0 个标签」。现在按你说的分工走：
  - **LBD 标签**：Python 从 **PDF 文字层**认（就是老流程 `pdf_extract.py` 那套，已内置在程序里），位置就是图纸上 LBD 文字的位置；
  - **STR 支架号**：从识别结果 JSON 生成，按所属 LBD 分组、组内行优先编号（和 `frame_detect/map_lbd_str.py` 规则一致，每组从 01 起，不在任何 LBD 里的支架不编号），两组行写进同一个提取文件。
- **STR 号改成逐页画**：以前不管多少页，只有起始页那一页会画支架号（而且用的是"面积最大的底图"）；现在每张底图配自己的页号，86 页的项目就是 86 页都画。
- 修掉 CAD 侧那个弹窗 **「PDF布局工具错误: 类型不正确 - "\n[AI] no labels."」**：没有标签可画时，`c:pdfgridai` 会对一句字符串做 `(fix ...)`；现在正常提示"这次没有可画的标签"。
- 识别结果 JSON 里没有可填的标签时，日志会直接说明原因，不再静默写空文件。

## 上一版（2.17.7）

- 打包版**连不上 CAD（`No module named 'pythoncom'`）**修好了：打包用的 Python 里补装 pywin32，exe 里带上 `pythoncom` / `win32com`；少了 pywin32 时界面会直接提示，`build.py` 打包前也会检查
- 打包不再覆盖仓库里的 `Voltage-CAD MAP.spec`（生成的 spec 放进 `build/`）

## 更早（2.17.6）

- 界面下拉框（标签背景色 / STR 背景色 / STR 背景开关 / 支架明细行的「拆不拆」）**禁用滚轮切换**：鼠标停在上面滚轮不再改值，只能点开列表选；滚轮改成正常滚动页面，避免翻页时误改设置
- 界面去掉「**网格编号**」页：那几格（行数/列距/前缀）本来就不参与执行流程，真正的网格编号参数在 CAD 插件的 PDFGRID 对话框里设；「生成执行计划」里也不再列这一条
- 「支架」页新增「**命名前缀**」：支架号前缀填 **`STR`**（编号形如 `STR01`、`STR02`…），默认就是 `STR`；`CIR` 是网格编号用的前缀，不是支架号的
- 改了前缀会带到 CAD：识别结果 JSON 里形如 `STRxx` 的支架号按新前缀改名后再画（填 `CIR` 就画成 `CIR01`），CAD 端 STR 号的背景填充也跟着新前缀认
- 配置/ini 新增 `rackPrefix`
- 标签位置**固定模型空间**：选项页的「模型空间 / 当前布局」单选拿掉，ini 恒为 `labelWhere=M`（老配置是 `L` 也忽略）。填到布局要按当前布局的最大视口换算坐标，布局多/视口不规则容易错位，而且 STR 号本来就一直画在模型空间
- 生成布局后**按 LBD 区域上下限对准视口**（选项页新开关，默认开）：从识别结果 JSON 取每页 LBD 区域（Node 大框）的合并范围，插件按整页对准完之后再用这个范围重新对准一次视口 —— 居中 + 按区域上下限等比缩放。实测 LBD 区域只占页面高约 60%、宽 26%~42%，按整页对准图会偏小、留一圈空白；这一步是等比缩放（不会拉变形），拿不到识别结果时自动跳过

## 更早（2.17.4）

- 「执行输出」时**自动导出识别范围表**：LBD 区域范围（区域名、所属逆变器、区域内支架数）和 Typical（支架）范围，坐标同时给原图像素与归一化值；输出 `lbd_regions.csv`、`typicals.csv`、`lbd_symbols.csv`、`pages.csv`、`lbd_typical.json`
- 新增顶部「**导出区域范围**」按钮，可随时按识别结果导一次
- 「支架」页新增**支架类型明细**：每个支架类型单独一行（支架类型 / 串数 / 长度FT / 数量），**类型自动读识别结果，不用手填**；每行的**拆不拆自己选**（不拆 / 2行 / 3行）
- 支架类型**自动写入 ini**：点「执行输出」时程序会把类型补写进配置，这一次 CAD 侧就能读到，无需先手动刷新
- 新增选项开关：「生成时自动导出 LBD区域/支架范围」「支架类型自动读识别结果(免手填)」；新增「区域范围输出目录(可空)」
- 识别结果 JSON 自动查找：界面选的识别结果 → AI 输出目录 → 提取文件所在目录，取最新一份能解析的；找不到只提示一句，不打断流程
- 新增 `编排器/lbd_regions.py`（区域范围与支架类型提取，随 exe 一起打包）；`frame_detect/extract_lbd_typical.py` 改为它的命令行入口

## 附件

- `Voltage-CAD MAP.exe`（Windows 单文件版，直接替换旧 exe 即可；也可以在程序里点「检查更新」自动升级）
