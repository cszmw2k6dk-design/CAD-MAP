v2.20.2：更新内容
1 修复 STR 号避让失效（关键）：PDF 图纸带 /Rotate 时，底图文字坐标和底图差一个 90°，
  避让框算到了错误位置 —— 拆分之后一排的号照样压在底图标号上。现在按页面旋转换算，
  实测同样的图纸：压住标签 112 处 -> 0 处
2 STR 号现在也避开 CAD 里画的 LBD 标签（LBD 名字画在识别区域中心，原来会被 STR 号盖住）
3 LBD 标签字高统一走「标签高度」那一格：默认 0.25 模型单位，想改就改那个数（0 = 自动按底图大小）
4 「生成布局后按 LBD 区域上下限对准视口」这一步加进度：进度条 + 「区域对准 3 / 12」+ 用时统计；
  修掉进度条被上一阶段（创建布局）拉回去、以及下方日志每 0.15 秒重复刷一条区域对准用时的问题
5 删掉没用的「使用AI自动识别(无需手动框)」和「锁定视口显示」两个选项
6 「执行输出」按钮挪到那一行的最右边

上一版（2.20.1）
1 修复更新自动保存json文件的bug

上一版（2.20）
1 修复支架拆分失效
2 删除多余没用按钮
3 加入自动把STR号放在同一高度功能
