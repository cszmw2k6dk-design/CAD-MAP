v2.53：更新内容
1 标注工具新增「用模型识别 / 识别全册」：用训练好的 YOLO 模型给图纸自动画框（本页或整册），
  类别按**模型自带的类别名**映射（修好 Node / Tracker 认反的问题），整册=文档所有页（修「开始：0 页」）
2 标注工具训练面板：新增「模型」下拉（yolov8n/s、yolo26n/s，也可选自己训好的 best.pt）+「续训(resume)」
  勾选；训练日志改成跨线程发信号（修训练时闪退）、日志上限 3000 行；修漏 import tempfile 导致一点就报错
3 标注工具界面：Node 蓝 / Tracker 红（颜色互换）、画图工具栏移到左侧、保存相关留在上面、
  两点画框（也可继续拖画）、缩放跟随十字光标、锁定只锁画布区、名字默认只显示选中的框、
  窗口标题显示版本+生成时间
4 标注工具：支架按「长度差 ≤10% 算同一类」整册分档，结果写进 JSON 的 raw.strings；
  导出 YOLO 数据集（images/labels/data.yaml）；导出 LBD 核对表 CSV
5 标注工具：设置与渲染缓存移到 %LOCALAPPDATA%\LBD标注工具\，不再在程序目录（比如桌面）旁边生成缓存

v2.53：更新内容
1 「导出 PDF」加上「质量」可调：质量(线条) 300 / 600 / 1200 / 2400 / 4800 dpi（或跟随设备），
  质量(图片) 跟矢量质量、或单独设。选完打印时把打印设备配置（.pc5）的 dpi 临时改掉、打完
  立刻改回原样（原文另外存一份在 %TEMP%\MAP-CAD-绘图仪配置备份）；默认「跟随设备」，
  这时界面上会把这台设备现在的 dpi 写出来，想改就在上面选
2 「导出 PDF」改成一次打完所有页面：一次把整份布局清单交给 CAD（SetLayoutsToPlot）+
  一次 PlotToFile 打完，不再一页一次地切布局反复调打印。ZWCAD 的 PDF 驱动对多布局是
  「一个布局存一个文件、按 图纸名_布局名.pdf 存」，所以打完再按布局顺序合成一份多页 PDF
  —— 它自己的批量打印工具也是这么做的（工具里那个选项就叫「将多个单页 PDF/DWF 文件合并
  为一个多页 PDF/DWF 文件」）；万一这个 CAD 不支持批量接口，才退回逐张打印并在结果里写明
3 「导出 PDF」不再让打印驱动每打一页弹一次 PDF 阅读器（打印期间把驱动自己的「打完自动
  打开文件」关掉），改成界面上可勾的「打完后打开 PDF」，只打开最终那一份；打完还会把布局的
  打印设备和纸型改回原来的，不在图纸里留下本次导出用的设置

v2.52：更新内容
1 标注工具新增「按框内文字补 LBD 编号」：直接从 PDF 文字层读框里的编号（不是模型认字），
  规则是框内文字优先归自己的框、框里只印裸数字也认、再兜底找文字里的数字；拆开的
  （框里只剩 LBD 或分表名）和被截断的（结尾是 - _ . ( #）一律过滤，不硬填错号
2 标注工具：已有名字默认不动，需要覆盖就勾「重算(覆盖已有名字)」；新增「导出核对表 CSV」，
  每个框一行（页号/分表/坐标/现有名字/框内候选/建议名字/来源），方便在 Excel 里核对
3 标注工具修复「拖动框之后保存不生效」：这个 PySide6 版本收不到 Qt 的位置变化事件，
  拖完坐标没写回数据；现在拖动和松手时主动同步（改大小/微移/改名/增删/复制粘贴都验过）
4 标注工具修复「打开 PDF 直接标注时点补全编号没反应」：那条路用的是空白文档对象，
  排页序的代码只认识别结果 JSON；现在两种模式都能补，分表名按页面上印的来认
5 标注工具：渲染缓存和设置文件改到 %LOCALAPPDATA%\LBD标注工具\，不再在程序目录（比如桌面）
  旁边生成 render_cache 和一堆 png；老设置会自动迁移
6 标注工具：LBD 名字默认只显示「选中的那个框」，不再满屏压在一起；工具栏可选 不显示/只看选中的/全部小中大
7 CAD 侧：LBD 分表名不再只认 INV+数字+字母+数字，改成先拿标签表里的分表名去文字里匹配
  （"1.01-LBD-17" 这种也认）、再用布局名（第几页=第几个分表）兜底；标签表 A 列没有
  "LBD-nn" 时按行序号取号，和标注工具用同一套键；LBD_STAT 多打一项「用布局名兜底=」
8 打包：两个打包脚本都支持 --onedir（文件夹版），启动比单文件快约 4 秒、不在 %TEMP% 留解压残留

v2.51：更新内容
1 修复「LBD 标签避让 STR 号」一直没生效：LBDAVOIDSTR 里拿两个列表直接比大小会报
  「用于比较的参数类型不正确」，跑一次挂一次（错误被静默吞掉）；现在改成逐项比较，
  避让正常执行，万一再出错也会在命令行打出原因
2 修复页号对不上、跑完什么都不画：「图纸页 → 底图顺序号」的重编号，在识别结果里
  每页都带一条空记录时会退化成 1:1，标签行的页号跟底图对不上；现在只把「真的有框 /
  真的有编号」的页算作图纸页
3 修复「只标了框、还没填 LBD 名字」时支架号一个都不画：支架号原来要求所属 LBD 区域
  必须有名字才编号，现在不再依赖名字；顺便两个区域重名时也不会再并成一组连着编号
4 修复兜底提取丢了页码映射：PDF 文字层那条路取支架号时补上底图顺序号，否则行写出来了、
  CAD 按页号也找不到
5 修复执行过程中状态栏一直跳「正在批量布局…」：连接 / 打开 DWG 阶段还没收到 CAD 的
  进度，轮询却按「批量布局」改写状态文字，现在这一阶段不再抢状态
6 四象限默认值改成「从近端起步·列优先」：右上=5、左上=6、左下=3、右下=1
  （原来是 4/8/7/2 行优先；界面上照样可以自己改）
7 新增配套的「LBD 标注工具」（源码在仓库 标注工具\ 目录）：给识别结果做人工修正 ——
  改框、改 LBD 名字、删多余框，改完另存一份同格式 JSON 给本程序用；也可以不依赖识别结果，
  直接打开一份 PDF 从零标注。它会自己找 poppler 渲染高分辨率底图（JSON 内嵌图不够清楚时）；
  支持复制/粘贴画好的框（Ctrl+C / Ctrl+V，可跨页粘贴，每贴一次自动错开），
  撤销（Ctrl+Z）和重做（Ctrl+Y）

上一版（2.50）
1 加入「根据 LBD 位置改每一个区域的排序方式」：以本页汇流箱(Box)为原点，看每个 LBD
  区域中心落在哪个象限，四个象限各选一种 STR 编号走向；没选的就按全局「STR 编号顺序」走
2 优化界面
3 删掉「PDF 底图锁定」：PDFLOCK / PDFUNLOCK 两个命令和自动上锁全部删除，插件不再把底图
  收进「PDF底图」层、也不再锁层；老图纸上已经被锁过的底图，在 CAD 里用图层管理器（LAYER）
  手动解锁那一层一次即可

上一版（2.40）
1 STR 号避让更彻底：以前只按「文字框」避让底图上本来就印着的 LBD 编号，现在连号的背景白底一起算，
  压住就继续上下挪；整排找不到公共位置的，改成这一排的号各自单独挪
2 新增「LBD 标签反过来避让 STR 号」：STR 号画完后自动跑一趟，用 CAD 里真实的包围盒判断，
  把压住号的 LBD 标签上下错开（手动执行 LBDAVOIDSTR 可以随时再跑一遍）
3 只画 STR 前缀的号：识别结果里带的类名（Node9、Typical 之类）不会再被画到图纸上
4 底图自动收进「PDF底图」层并锁定：在模型空间里改标号时不会误点到压在最下面的 PDF 底图；
  要移动或缩放底图先执行 PDFUNLOCK，改完执行 PDFLOCK 锁回去
5 新增「导出 PDF」：勾选要导的布局 + 选 PDF 打印设备和纸型，逐张打印后合并成一份多页 PDF
6 更新下载改成测速选源 + 卡住自动换源 + 断点续传，并显示已下载大小和速度
7 界面「目标 DWG 文件」改名「模板 DWG 文件」

上一版（2.30）
1 修复 STR 号避让失效
2 加 STR 号避开 CAD 里画的 LBD 标签
3 生成布局后按 LBD 区域上下限对准视口过程加进度条
4 删掉没用的「使用AI自动识别(无需手动框)」和「锁定视口显示」两个选项
5 「执行输出」按钮挪到那一行的最右边

上一版（2.20.1）
1 修复更新自动保存json文件的bug

上一版（2.20）
1 修复支架拆分失效
2 删除多余没用按钮
3 加入自动把STR号放在同一高度功能
